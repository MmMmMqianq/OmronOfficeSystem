name_list = ["张三", "李四", "王五", "王小二", "张三"]

# len(legth长度) 函数可以统计列表元素的总数
list_len = len(name_list)
print("列表中包换的元素为 %d" % list_len)

# count 方法可以统计列表中某一个元素出现的次数
count = name_list.count("张三")
print("张三出现的次数为 %d" % count)

